<UserControl x:Class="centre_soutien.Views.GestionSallesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:centre_soutien.Views"
             xmlns:viewModels="clr-namespace:centre_soutien.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Name="GestionSallesUserControl">

    <d:UserControl.DataContext>
        <viewModels:SalleViewModel/>
    </d:UserControl.DataContext>

    <UserControl.Resources>
        <!-- Style pour les boutons -->
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Margin" Value="0 5"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Background" Value="#FF3A4F7A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Color="#40000000"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4A6FA5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style pour les TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
        </Style>

        <!-- Style pour le DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Margin" Value="0 0 0 15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEE"/>
            <Setter Property="VerticalGridLinesBrush" Value="#EEE"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF3A4F7A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre avec effet d'ombre -->
        <TextBlock Grid.Row="0" Text="Gestion des Salles" 
                   FontSize="24" FontWeight="SemiBold" Margin="0 0 0 15"
                   Foreground="#FF3A4F7A">
            <TextBlock.Effect>
                <DropShadowEffect BlurRadius="5" ShadowDepth="1" Color="#20000000"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- DataGrid avec style amélioré -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Salles}"
                SelectedItem="{Binding SelectedSalle, Mode=TwoWay}"
                AutoGenerateColumns="False" IsReadOnly="True"
                CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IDSalle}" Width="Auto"/>
                <DataGridTextColumn Header="Nom/Numéro" Binding="{Binding NomOuNumeroSalle}" Width="*"/>
                <DataGridTextColumn Header="Capacité" Binding="{Binding Capacite}" Width="Auto"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                <DataGridCheckBoxColumn Header="Archivée" Binding="{Binding EstArchivee}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Formulaire avec carte visuelle -->
        <!-- Formulaire avec carte visuelle - Version corrigée -->
        <Border Grid.Row="2" BorderThickness="0" CornerRadius="5" Margin="0 0 0 15"
                Background="White">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#20000000"/>
            </Border.Effect>
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Nom/Numéro:" VerticalAlignment="Center"
                       Margin="5" FontWeight="SemiBold"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NomSalleInput, UpdateSourceTrigger=PropertyChanged}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Capacité:" VerticalAlignment="Center"
                       Margin="5" FontWeight="SemiBold"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CapaciteInput, UpdateSourceTrigger=PropertyChanged}"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Description:" VerticalAlignment="Center"
                       Margin="5" FontWeight="SemiBold"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding DescriptionInput, UpdateSourceTrigger=PropertyChanged}" 
                         TextWrapping="Wrap" AcceptsReturn="True" MinLines="2"/>

                <StackPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="2" Orientation="Vertical" 
                            Margin="15 0 0 0" VerticalAlignment="Top">
                    <Button Content="Ajouter" Command="{Binding AddSalleCommand}"/>
                    <Button Content="Modifier" Command="{Binding UpdateSalleCommand}"/>
                    <Button Content="Archiver" Command="{Binding ArchiveSalleCommand}"/>
                    <Button Content="Nouveau" Command="{Binding ClearFormCommand}" Margin="0 15 0 0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Barre de statut améliorée -->
        <StatusBar Grid.Row="3" Background="#FF3A4F7A" Foreground="White">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" FontWeight="SemiBold"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>