<UserControl x:Class="centre_soutien.Views.GestionProfesseursView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:centre_soutien.Views"
             xmlns:viewModels="clr-namespace:centre_soutien.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1200"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <!-- Styles communs -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12 6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Background" Value="#FF3A4F7A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Color="#40000000"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4A6FA5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Margin" Value="0 8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Style pour le DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Margin" Value="0 0 0 15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF3A4F7A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <d:UserControl.DataContext>
        <viewModels:ProfesseurViewModel/>
    </d:UserControl.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0" Text="Gestion des Professeurs" 
                   FontSize="24" FontWeight="SemiBold" Margin="0 0 0 15"
                   Foreground="#FF3A4F7A">
            <TextBlock.Effect>
                <DropShadowEffect BlurRadius="5" ShadowDepth="1" Color="#20000000"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" 
                 ItemsSource="{Binding Professeurs}"
                 SelectedItem="{Binding SelectedProfesseur, Mode=TwoWay}"
                 AutoGenerateColumns="False" 
                 IsReadOnly="True"
                 CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IDProfesseur}" Width="Auto"/>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" Width="*"/>
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Telephone}" Width="*"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="2*"/>
                <DataGridCheckBoxColumn Header="Archivé" Binding="{Binding EstArchive}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Formulaire -->
        <Border Grid.Row="2" BorderThickness="0" CornerRadius="5" Margin="0 0 0 15"
                Background="White">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#20000000"/>
            </Border.Effect>
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Grid Grid.Column="0" Margin="0 0 16 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Nom:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NomInput, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Prénom:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PrenomInput, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="Téléphone:"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TelephoneInput, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="3" Grid.Column="0" Content="Notes:"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding NotesInput, UpdateSourceTrigger=PropertyChanged}" 
                             TextWrapping="Wrap" AcceptsReturn="True" MinLines="3"/>
                </Grid>

                <StackPanel Grid.Column="1" Orientation="Vertical" MinWidth="200">
                    <Button Content="Ajouter" Command="{Binding AddProfesseurCommand}"/>
                    <Button Content="Modifier" Command="{Binding UpdateProfesseurCommand}"/>
                    <Button Content="Archiver" Command="{Binding ArchiveProfesseurCommand}"/>
                    
                    <Separator Margin="0 12" Background="#FFE0E0E0"/>
                    
                    <Button Content="Gérer Matières/Rémun." Command="{Binding OpenGestionMatieresProfesseurCommand}"/>
                    
                    <Separator Margin="0 12" Background="#FFE0E0E0"/>
                    
                    <Button Content="Nouveau/Vider" Command="{Binding ClearFormCommand}" 
                            BorderBrush="#FF3A4F7A" BorderThickness="1" 
                            Foreground="#FF3A4F7A" Background="Transparent"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Barre de statut -->
        <StatusBar Grid.Row="3" Background="#FF3A4F7A" Foreground="White">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" FontWeight="SemiBold"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>