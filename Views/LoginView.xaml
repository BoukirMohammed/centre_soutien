<Window x:Class="centre_soutien.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:centre_soutien.Views"
        xmlns:viewModels="clr-namespace:centre_soutien.ViewModels"
        mc:Ignorable="d"
        Title="Connexion - Gestion Centre de Soutien" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        FocusManager.FocusedElement="{Binding ElementName=LoginTextBox}"> <!-- Met le focus sur le TextBox de login au démarrage -->

    <!-- Définition du DataContext directement en XAML -->
    <Window.DataContext>
        <viewModels:LoginViewModel/>
    </Window.DataContext>

    <Border Padding="20" BorderBrush="LightGray" BorderThickness="1" Margin="10">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Authentification" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Login :" Margin="0,0,0,5"/>
            <TextBox x:Name="LoginTextBox" Text="{Binding LoginInput, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,10"/>

            <TextBlock Text="Mot de passe :" Margin="0,0,0,5"/>
            <!-- Nommer le PasswordBox pour y accéder depuis le CommandParameter -->
            <PasswordBox x:Name="UserPasswordBox" Padding="5" Margin="0,0,0,20"/>

            <Button Content="Se Connecter" 
                    Command="{Binding LoginCommand}" 
                    CommandParameter="{Binding ElementName=UserPasswordBox}" 
                    IsDefault="True" Padding="10,5" HorizontalAlignment="Stretch"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" MinHeight="30" Margin="0,10,0,0" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>